export interface AnimationVideoTheme {
  backgroundColor: string;
  textColor: string;
  badgeBg: string;
  badgeBorder: string;
  badgeText: string;
  accentColor: string;
}

export const DEFAULT_ANIMATION_VIDEO_THEME: AnimationVideoTheme = {
  backgroundColor: "#020202",
  textColor: "#FFFFFF",
  badgeBg: "rgba(255,215,0,0.1)",
  badgeBorder: "#FFD700",
  badgeText: "#FFD700",
  accentColor: "#FFD700",
};